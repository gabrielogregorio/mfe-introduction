import { ReactNode } from "react";
export declare const LinkNavigationWithSound: ({ to, content, fontSize, isActive, }: {
    to: string;
    content: ReactNode;
    fontSize?: string;
    isActive?: boolean;
}) => JSX.Element;
